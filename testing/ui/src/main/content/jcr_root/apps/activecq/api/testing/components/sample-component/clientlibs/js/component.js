$(function() {
    $('.sample-component').each(function() {
        /*
         * Handle component logic here.
         *
         * If specific data must be passed in, supply via data-* attributes.
         *
         *     var x = $(this).find('.js-data').data('param-1');
         *
         */
    });
});